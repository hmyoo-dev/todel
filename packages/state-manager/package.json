{
  "name": "state-manager",
  "version": "0.1.0",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "build:watch": "tsc --build --watch",
    "test": "jest",
    "coverage": "jest --coverage"
  },
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "jest": "^26.6.2",
    "typescript": "^4.0.5"
  }
}
