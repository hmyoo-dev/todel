{
  "version": "independent",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "packages": ["packages/*", "examples"],
  "command": {
    "version": {
      "allowBranch": "main",
      "noPrivate": true,
      "ignoreChanges": ["**/tests/**", "**/*.md"]
    }
  }
}
